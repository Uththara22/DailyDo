<resources>
    <!-- Android 12+ splash screen background override (platform attr) -->
    <style name="Theme.DailyDo" parent="Base.Theme.DailyDo">
        <item name="android:windowSplashScreenBackground">@color/white</item>
    </style>
</resources>

